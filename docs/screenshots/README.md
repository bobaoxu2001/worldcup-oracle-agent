# Screenshots — place 4 PNGs here before Devpost submission

The main `README.md` references these exact filenames. Capture them locally
(`npm run dev` → http://localhost:3000) and save them here:

| File | What to capture |
|------|-----------------|
| `home.png` | The home page / agent chat with the hero, proof-point chips, and suggested prompts (the flagship prompt should be the first chip). |
| `agent-timeline.png` | After running **"Who will win Argentina vs Germany based on the latest team news?"** — the **Agent reasoning** timeline (Gather data → Resolve daily news → Injury/squad impact → Run Monte Carlo → Report). |
| `news-impact.png` | The **Latest News Impact** card showing the **Base vs news-adjusted** probability table + the "Impact:" explanation + source/demo badge. |
| `memory-center.png` | The **Agent Memory Center** at `/memory` — memory backend, news intelligence status, "Why MongoDB matters", and recent sessions. |

Tips:
- Use a wide viewport (~1280px) and the dark theme (default).
- For a richer Memory Center shot, run a prediction first so a session appears.
- Optional: a short GIF of the flagship flow named `flagship.gif` also works well on Devpost.
